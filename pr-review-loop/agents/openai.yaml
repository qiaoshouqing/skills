interface:
  display_name: "PR Review Loop"
  short_description: "Poll PR reviews, fix feedback, stop when merge-ready."
  default_prompt: "Use $pr-review-loop to submit a PR, poll review feedback every few minutes, fix actionable comments, and stop when the PR is merge-ready."
policy:
  allow_implicit_invocation: true
